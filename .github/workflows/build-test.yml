name: 'Build Test'
on: [push]

jobs:
  build:
    name: 'Check Build'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          yarn
      - run: |
          yarn build
